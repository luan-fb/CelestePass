<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base do aplicativo -->
    <style name="Base.Theme.CelestePass" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cores principais -->
        <item name="colorPrimary">@color/cruzeiro_blue</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Cores secundárias -->
        <item name="colorSecondary">@color/cruzeiro_blue</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Containers (Material 3) -->
        <item name="colorPrimaryContainer">@color/cruzeiro_blue_dark</item>
        <item name="colorSecondaryContainer">@color/cruzeiro_blue_dark</item>

        <!-- StatusBar -->
        <item name="android:statusBarColor">@color/cruzeiro_blue_dark</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Fullscreen -->
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
